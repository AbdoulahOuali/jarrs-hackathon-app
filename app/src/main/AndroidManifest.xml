<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ingenico.epayments.jarrs.hackaton.oqp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ingenico_brand_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SplashScreenActivity"
            android:label="@string/title_splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/title_registration_activity"
            android:parentActivityName=".activity.SplashScreenActivity">
            <!-- Parent activity meta-data to support API level 4+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ingenico.epayments.jarrs.hackaton.oqp.activity.SplashScreenActivity" />
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_home_screen"
            android:parentActivityName=".activity.LoginActivity">
            <!-- Parent activity meta-data to support API level 4+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ingenico.epayments.jarrs.hackaton.oqp.activity.LoginActivity" />
        </activity>
        <activity
            android:name=".activity.UserRegistrationActivity"
            android:label="@string/title_registration_activity"
            android:parentActivityName=".activity.LoginActivity">
            <!-- Parent activity meta-data to support API level 4+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.ingenico.epayments.jarrs.hackaton.oqp.activity.LoginActivity" />
        </activity>
    </application>
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <uses-permission android:name="android.permission.NFC" />
</manifest>